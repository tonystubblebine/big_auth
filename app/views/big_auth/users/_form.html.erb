<%= form_for(@big_auth_user) do |f| %>
  <% if @big_auth_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@big_auth_user.errors.count, "error") %> prohibited this big_auth_user from being saved:</h2>

      <ul>
      <% @big_auth_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
