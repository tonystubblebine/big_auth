<h1>Roles</h1>

<table>
  <tr>
    <th>User</th>
    <th>Role</th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.name %></td>
    <td><%= render "form", :roles_user => (user.roles_users.first || BigAuth::RolesUser.new(:user => user)) %></td>
  </tr>
<% end %>
</table>

<br />

