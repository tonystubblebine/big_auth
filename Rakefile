begin
  require "jeweler"
  Jeweler::Tasks.new do |gem|
    gem.name = "big_auth"
    gem.summary = "Rails engine for handling authentication."
    gem.email = "tony+bigauth@tonystubblebine.com"
    gem.authors = ["Tony Stubblebine"]
    gem.files = Dir["{lib}/**/*", "{app}/**/*", "{config}/**/*"]
  end
rescue
  puts "Jeweler or dependency not available."
end
